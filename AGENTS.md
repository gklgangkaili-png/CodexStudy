## Agent skills

### 问题跟踪器

问题和规格在本仓库的 GitHub Issues 中管理。详见 `docs/agents/issue-tracker.md`。

### 分类标签

使用五个默认分类标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

使用 single-context（单上下文）领域文档布局。详见 `docs/agents/domain.md`。
