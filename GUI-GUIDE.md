# 图形控制中心

## 启动

在仓库根目录运行：

```powershell
py -m game_demo_automation.workflow_gui_safe_v19
```

启动时先选择 Maa 录制比例：16:9（1280×720）或 4:3（960×720）。目标游戏窗口的客户区必须与所选尺寸一致。

## 操作顺序

1. 点击“选择区域并开始录制”，在目标游戏客户区内拖动定位固定尺寸录制框。
2. 完成一段完整示教后停止录制。
3. 点击“校准并导出”，检查状态名称、ROI 和匹配阈值。
4. 选择 Maa 任务包保存目录并完成导出。
5. 在任务包列表中选择需要执行的任务包。
6. 设置循环次数和每轮等待秒数。
7. 保持目标游戏在前台，启动 Maa 执行。
8. 执行期间或轮间等待时按 `F11`，停止当前任务并取消剩余循环。

`recordings/` 和 `tasks/` 是本地运行数据，已通过 `.gitignore` 排除，不应提交到仓库。
